<%= form_for @attempt do |f| %>
  <%= render 'shared/error_messages', target: @attempt %>
	<fieldset>
		<%= f.label :score %>
		<%= f.text_field :score %>
		<br>
		<%= f.submit "Record Attempt", 
        class: "btn btn-small btn-primary" %>		            
	</fieldset>
<% end %>
